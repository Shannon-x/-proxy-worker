{
  "name": "proxy-worker",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "publish": "wrangler deploy"
  },
  "keywords": ["proxy", "cloudflare", "worker"],
  "author": "",
  "license": "ISC",
  "description": "代理IP自动采集与展示系统",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0"
  }
}
